{
  "name": "sui-x402-facilitator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "x402 v2 facilitator for the Sui `exact` scheme — non-custodial: verifies (dry-run) and settles (broadcasts) the payer's own signed transaction. Never holds funds.",
  "scripts": {
    "serve": "tsx src/index.ts",
    "e2e": "tsx scripts/e2e.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "^2.17.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
